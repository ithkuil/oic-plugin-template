#!/bin/bash

# This code gets installed and run in the index container
# The index container acts as a database and possibly a controller
# For example, storing a list of VMs and containers, services
# Or acting as a master for a distributed file system
